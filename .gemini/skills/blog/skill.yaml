depends:
  - SHARED/change-framework
  - SHARED/review-protocol
tags:
  - writing
  - blog
category: content
targets:
  claude: true
  codex: true
# Optional documentary style-guide path, not a runtime dependency.
portability_allow:
  - "~/.claude/blog/*"
